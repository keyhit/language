%p#notice= notice
%h1 Langs
%table.table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @langs.each do |lang|
      %tr
        %td= lang.name
        %td= link_to 'Show', lang
        %td= link_to 'Edit', edit_lang_path(lang)
        %td= link_to 'Destroy', lang, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Lang', lang_path(current_user.id)
